/**
 * @deprecated This component was used when auth was localStorage-based.
 * Auth is now handled server-side via JWT cookies. This file is kept only
 * to avoid breaking any stale imports; it renders nothing.
 */
export default function SeedAdmin() {
  return null;
}
