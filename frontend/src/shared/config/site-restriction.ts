// Temporary lockdown: only /jobs is servable while this is true.
// Flip to false to restore normal routing and header behavior with no other code changes.
export const SITE_RESTRICTED_TO_JOBS = true;
